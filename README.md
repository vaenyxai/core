# core (placeholder)

Future home of the Vaenyx **public MIT engine** — the Method/Routine runtime,
loader, schema validation, contentHash, contracts, gateway/API scaffolding,
basic local execution, per-project memory, and the Method/plugin SDK.

**Not split yet.** The engine currently lives inside the monolithic private
`vaenyx` app repo (working copy at `C:\Vaenyx`). This is a reserved placeholder;
code moves here at the **open-core split milestone** — when the core API
stabilizes and the code is physically separable (DECISIONS 2026-06-23). This repo
**flips to public / MIT** at that point.
